# ENV EXAMPLE - copy to .env and fill values
NODE_ENV=development

# Supabase (Postgres)
SUPABASE_URL=https://your-supabase-url.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
DATABASE_URL=postgresql://user:pass@db:5432/priming

# Redis
REDIS_URL=redis://redis:6379

# JWT / Auth
JWT_SECRET=replace_with_a_strong_secret
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=replace_with_refresh_secret

# OpenAI / Gemini
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=ya29...

# Render deploy secrets (placeholders for GitHub Actions)
RENDER_API_KEY=render_key_here
RENDER_SERVICE_ID_AUTH=render_service_id_auth
RENDER_SERVICE_ID_TUTORING=render_service_id_tutoring

# Domains
DEV_DOMAIN=rosybrown-crow-726051.hostingersite.com
PROD_DOMAIN=theprimingparagon.org

# Other service toggles
VECTOR_DB=chroma
QUEUE=redis

---

## 3) Shared Python schemas (`shared/`)

### `shared/pyproject.toml`
```toml
[project]
name = "priming-shared-schemas"
version = "0.1.0"
dependencies = [
  "pydantic>=2.6.0"
]
