Place Kubernetes manifests for production here. Example manifests:
- gateway.yaml
- tutoring-engine.yaml
- pedagogy-ingest.yaml
- postgres (statefulset) or use managed Cloud SQL
Secrets should be stored in a secure store (GCP Secret Manager, AWS Secrets Manager).
